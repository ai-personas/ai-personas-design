# Continuing through learning, cooperation and infrastructure failure

[Design chapters](README.md) · [Contracts](../implementation/CONTRACTS.md) · [Acceptance](../evaluation/ACCEPTANCE.md)

This normative clarification refines existing memory, cooperation, action and recovery responsibilities. It introduces no task classifier, profession roster, compulsory tool, or guarantee of human-like behavior. Specifying the behavior is not evidence that it has been demonstrated.

## Learning remains a persona decision

Each ordinary persona decision considers useful work, possible reusable learning, organization and the next context together. A plan, question, failed attempt or social interaction can support a tentative or attributed fragment before task success. Its prose is authored through the persona's current character, in its own natural voice. Character influences attention and interpretation; it does not turn uncertainty into fact or require trait labels in every sentence.

Deferring a valuable idea creates a persistent opportunity with an identity, a cue, a reason, a reconsideration condition and any received evidence. Omitting it from a later decision does not dismiss it. The persona may update it, explicitly dismiss it with a reason, or address it through an actually committed fragment or organizational change. An intention to retain is distinct from retained content. A pending opportunity alone schedules no reflection call and establishes no lesson quota.

A compact working intention can preserve the proposed useful outcome, fidelity, consequential unknowns, next evidence and collaboration approach across decisions. It is neither accepted scope nor permission to commit another person. Research breadth, depth, peer engagement and restraint remain conditioned by character, current context and experience. Search is the primary discovery affordance for unfamiliar domain needs, current external facts and tool suitability; relevant existing evidence can make another search unnecessary. The mechanism must not prescribe a particular engineering program or domain checklist.

## Shared questions retain a readable history

A work-visible question permits attributable peer answers, conditional assumptions and challenges, even when addressed to the human. Only an explicitly required human authorization or unavailable outside fact requires that human contribution. An assumption remains conditional and cannot impersonate consent.

Assessing replies and resolving a question must not silently narrow the audience of the original shared question. Check the proposed conclusion's source permissions against its intended audience before committing. If disclosure is unavailable, keep the readable question and replies intact and preserve the failed delivery diagnostic. Resolution is an attributable conclusion distinct from the original question.

Derived content retains restrictions from information actually supplied to the author. Runtime navigation identities and accounting receipts are not additional reads of unsupplied payload. Authored previews, copied text, source-bearing observations and selected fragments still carry their applicable restrictions. An untrusted result cannot declare itself exempt metadata.

A persona can prepare a fresh context for a work audience by explicitly clearing active private selections, handoff and private navigation. Retained owned learning remains stored. Unread private inputs are neither acknowledged nor resolved. This boundary cannot retroactively sanitize an earlier artifact or same-decision output; the new decision must use permitted information independently. Diagnostics should identify owned sources whose policy can be changed without exposing another person's private content.

Repeated failed lookups with the same target and policy state should reuse their recorded failure after a declared small bound. A changed source or permission can reopen the lookup. Suppression never grants access or resolves an effect. The persona still chooses an owner request, permitted alternative, independent progress or an honest wait.

## Infrastructure recovery preserves agency and effects

Transport timeouts, temporary provider unavailability and recoverable infrastructure faults are scheduling conditions. They must not silently become a voluntary persona wait. Declare a small immediate retry allowance, then continue spaced health checks with backoff and coordination across users of the same provider. Respect server retry timing. A health probe that needs inference is a fresh funded call, not free infrastructure activity.

Confirmed quota exhaustion stops automatic inference retries; credential failures require credential repair and must remain visible. Recovery must obey current resources and authority. Uncertain charges remain accounted. No saved action batch, uncertain external effect, or canceled decision is replayed to test health. A failed transport does not count as a completed semantic learning or context-repair attempt.

Recovery state and schedules survive restart. Pause, cancellation, removal and retirement prevail over late provider results and recovery timers. Apply the same distinction to initial character generation, model discovery, ordinary decisions and context maintenance. The interface distinguishes automatic recovery, credential repair, exhausted quota and requests for human input.

Read-only interface activity should use consistent snapshots without holding the writer reservation. Shared reads may be coalesced while in flight; closing one reader must not cancel another reader's request. Expensive file and memory views load when opened and release subscriptions and payloads when closed.

## Evidence required

Mechanical checks cover interrupted initialization, timed-out decisions, shared recovery probes, quota and credential dispositions, restart, cancellation races, uncertain accounting, unchanged failed lookups, shared-resolution rollback, source withdrawal and private-input preservation. Learning checks demonstrate durable deferral, explicit disposition, atomic graph edits and actual next-context inclusion.

Behavioral evidence remains separate: an unchanged short creation brief should reveal what personas actually investigate, produce, check and learn. Evaluate native editability and substantive quality against declared criteria; attractive previews and passing mechanism tests are insufficient. Reuse comparisons must distinguish retention, retrieval and measured benefit. Character comparisons require repeated observations and controlled conditions before attributing differences to OCEAN or modeled VAD.
